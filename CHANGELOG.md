## Changelog (Upcoming Release since 0.0.6) (9/13/2026)

* fix: resolve health & lint errors and add type augmentation (7c0ec90)
* fix: preserve kambasTags when converting image nodes between embedded base64 data URLs and vault media files (79edbf6)
* fix: resolve filter panel scroll snap reset, selection guard canvas alignment leakage, and accent color extraction palette depth (9792ac6)
* fix: monkeypatch canvas.selectAll while filter active to explicitly omit hidden nodes (4c98ac9)
* fix: restore !important to kambas-tag-hidden to ensure canvas node inline/theme styles do not override filter dimming (5a937f9)
* fix: resolve prefer-instanceof lint warning and clean up !important declarations in main.css (496d666)
* feat: include/exclude filter mode, in-view intelligence, cross-highlighting, README rewrite (3194858)
* feat(settings): add native toggle setting and command palette action to toggle auto-zoom on tag selection (6256b34)
* fix(panel): auto-refresh filter panel on undo/redo & enlarge trash delete button size (a47380d)
* fix(panel): clamp tag filter panel within container bounds on resize, and make tag search input sticky on top (ec0253c)
* feat(tags): format multi-word comma inputs into kebab-case tags and strip trailing punctuation (dbb3821)
* feat(tags): render tag badges outside below elements by default; add tagBadgePosition setting; add 'Toggle tag visibility' command palette action; auto-apply tags on Enter in TagModal (7b35fd6)
* fix(tags): auto-prune orphan active tag filters when tags no longer exist on canvas nodes, and add reset button in empty filter panel (c1642eb)
* feat(tags): reorder tag modal layout to place input box above tag chips; fix multi-node tag diff application and tag union pre-filling (3eb1279)
* fix: make auto-zoom-to-fit trigger only once on explicit tag filter toggle, allowing full zoom/pan afterwards; isolate tag filters per canvas file (f3b09cf)
* feat: auto zoom-to-fit on tag filter toggle/clear (5690e97)
* feat: tag filter panel v2 + health fixes (ad84b11)
