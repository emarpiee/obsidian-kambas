{
	"name": "kambas",
	"version": "0.1.0",
	"description": "Extend core plugin Canvas with PureRef-like image controls, inline Base64 embedding, and adjustable navigations.",
	"main": "main.js",
	"scripts": {
		"typecheck": "tsc --noemit",
		"dev": "node esbuild.config.mjs",
		"build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
		"lint": "eslint ./src",
		"lint:fix": "eslint ./src/**/*.ts --fix",
		"prettier": "prettier --write \"./src/**/*.{ts,tsx}\"",
		"clean": "npm run prettier && npm run lint:fix",
		"health": "npm run lint && npm run typecheck",
		"changelog": "node generate-changelog.mjs",
		"bump": "node version-bump.mjs",
		"release": "node release.mjs",
		"release:patch": "npm run build && npm run health && npm version patch && npm run bump && npm run release",
		"release:minor": "npm run build && npm run health && npm version minor && npm run bump && npm run release",
		"release:major": "npm run build && npm run health && npm version major && npm run bump && npm run release"
	},
	"keywords": [],
	"author": "emarpiee",
	"license": "MIT",
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@trivago/prettier-plugin-sort-imports": "^6.0.2",
		"@types/node": "^25.6.0",
		"esbuild": "^0.28.0",
		"eslint": "^10.7.0",
		"eslint-plugin-obsidianmd": "^0.4.1",
		"globals": "^17.7.0",
		"jsdom": "^29.1.1",
		"obsidian": "1.10.0",
		"prettier": "3.8.3",
		"ts-morph": "^28.0.0",
		"tslib": "^2.8.1",
		"typescript": "^6.0.3",
		"typescript-eslint": "^8.64.0"
	},
	"dependencies": {
		"yaml": "^2.4.5"
	}
}
